import { Button } from "@/components/ui/button";

export default function CTA() {
    return (
        <div className="w-full z-10 flex justify-center mt-10">
            <Button className="text-md font-thin">Add a todo</Button>
        </div>
    );
}
